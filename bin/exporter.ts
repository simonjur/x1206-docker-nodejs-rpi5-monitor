/**
 * A prometheus exporter of X1206 board metrics:
 * - capacity (percentage)
 * - voltage (voltage)
 * - ac power state (on/off)
 * - charging state (enabled/disabled)
 */
import "../src/exporter.ts";
